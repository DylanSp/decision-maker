import { SFC } from 'react';
import * as React from 'react';
import { PasswordEntry } from './PasswordEntry';

export const VoteJoinModal: SFC<{}> = () => {
    return (
        <PasswordEntry />
        // join button
        // cancel buton
    );
}